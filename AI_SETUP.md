# TOMI AI features

تمت إضافة طبقة ذكاء اصطناعي اختيارية فوق ميزات TOMI الحالية. تعمل المساعدة الأساسية، الأسئلة الشائعة، البحث الخاص، الاقتراحات، وفحص الأمان المحلي بدون API key؛ لذلك لا يتوقف الموقع إذا لم تُضف خدمة خارجية.

## الميزات

- مساعد استخدام TOMI والدعم الذكي بالعربية العراقية.
- كشف محلي للسبام والروابط المختصرة/المشبوهة والاحتيال والإساءة والتهديدات.
- تنبيه المالك أو المشرف الذي يملك `manage_ai_safety` لمراجعة المحتوى. لا يتم حظر المستخدم تلقائياً.
- بحث داخل الرسائل التي يملك المستخدم صلاحية الوصول إليها فقط.
- حفظ اهتمامات المستخدم واقتراح غرف عامة وأصدقاء مناسبين.
- سؤال مساعد TOMI من الواجهة العائمة.
- أسئلة تحليلات الإدارة عن الضغط، المستخدمين الجدد، الغرف، الإحالات، والارتفاعات غير الطبيعية.

## تفعيل نموذج خارجي اختيارياً

أضف القيم التالية في Render Environment. لا تضع المفتاح داخل HTML أو JavaScript:

```env
AI_REMOTE_ENABLED=true
AI_API_KEY=ضع_المفتاح_هنا
AI_API_URL=https://api.openai.com/v1/chat/completions
AI_MODEL=gpt-4o-mini
AI_TIMEOUT_MS=8000
```

فحص الأمان الخارجي منفصل ومغلق افتراضياً:

```env
AI_REMOTE_SAFETY_ENABLED=false
```

عند تفعيله، قد تُرسل الرسائل النصية إلى مزود النموذج للفحص؛ لذلك فعّله فقط بعد اعتماد سياسة الخصوصية المناسبة. البحث الذكي لا يرسل تاريخ المحادثات إلى المزود، بل يعمل محلياً على الرسائل التي يملك المستخدم صلاحية الوصول إليها.

## صلاحية المشرفين

من إدارة المنصة > الصلاحيات امنح المشرف:

`manage_ai_safety` — مراجعة تنبيهات الأمان والسبام الذكية.

المالك يبقى لديه كل الصلاحيات تلقائياً. التنبيهات تحفظ في MongoDB ضمن حالة المنصة وبحد أقصى 500 تنبيه حتى لا يتضخم استهلاك الذاكرة على Render.

## ملاحظات الاستقرار

فحص الأمان يبدأ بعد إرسال الرسالة ولا يوقف الإرسال أو رفع الفيديو. لا يتم تشغيل تحليل AI تلقائياً على ملفات الفيديو، لأن ذلك قد يرفع استهلاك الذاكرة ويؤثر على الرفع.
