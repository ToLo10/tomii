# تحليلات TOMI ونظام الإحالة

تمت إضافة لوحة التحليلات على:

```text
/analytics.html
```

تظهر الصفحة للمالك `tomi` تلقائياً. ويمكن لمالك المنصة منح الصلاحيات التالية للمشرفين من لوحة الصلاحيات:

- `view_analytics` — عرض الملخص والمخططات.
- `view_system_metrics` — عرض الذاكرة، الحمل، ووقت استجابة السيرفر.
- `view_user_analytics` — عرض إحصائيات المستخدمين.
- `manage_referrals` — عرض إحصائيات روابط الإحالة.

## رابط الإحالة

كل حساب يحصل على رمز خاص تلقائياً. يستطيع المستخدم نسخ الرابط من صفحة الإعدادات، ويكون شكله مثل:

```text
https://your-domain.example/r/USER-1234ABCD
```

تُحسب الزيارات، الزيارات الفريدة، الزيارات المكررة، التسجيلات ونسبتا التحويل (مقابل كل الزيارات ومقابل الفريدة) داخل الفترة التي تختارها من لوحة التحليلات. التسجيل هنا يعني إنشاء حساب `Username + Password` فقط؛ لا يوجد اعتماد على بريد إلكتروني أو رقم هاتف.

تظهر تحت كل رمز قائمة الحسابات التي نُسبت إليه مع اسم المستخدم ووقت التسجيل والحالة. إذا ظهرت بصمة متصفح/جهاز واحدة مع أكثر من حساب، تُعرض كـ«تحتاج مراجعة» فقط؛ هذه إشارة احتمالية وليست إثباتاً أن الشخص نفسه أنشأ الحسابات.

عند توفر أحداث MongoDB تكون أرقام الفترة محسوبة من الأحداث نفسها. وإذا لم تتوفر أحداث محفوظة بعد، تُعرض البيانات المتاحة من سجل الإحالة مع وسم واضح في اللوحة حتى لا تختلط الأرقام التراكمية بأرقام الفترة.

مهم: «الزائر الفريد» في الملخص هو بصمة متصفح/جلسة تقريبية، بينما «الحساب الجديد» هو سجل Username جديد. لذلك لا يلزم أن يتساويا، ولا يمكن إثبات هوية شخص واحد بدقة من دون بريد أو هاتف أو وسيلة تحقق إضافية.

## Collections الجديدة في MongoDB

- `analytics_events` — أحداث محدودة المدة مع TTL.
- `analytics_rollups` — تجميعات كل ساعة وكل يوم.
- `analytics_user_stats` — عدادات نشاط المستخدمين بدون كلمات المرور أو محتوى الرسائل.
- `analytics_referrals` — إحصائيات روابط الإحالة.
- `analytics_system_metrics` — عينات الذاكرة والحمل ووقت event loop.

تمنع لوحة الملخص جمع تجميعات الساعة واليوم معاً للفترة نفسها؛ تختار نوع التجميع المناسب حتى لا تتضاعف أرقام المستخدمين الجدد أو الزيارات.

لا تُحفظ كل نبضة حضور كوثيقة مستقلة، ولا تُرسل نصوص المحادثات إلى التحليل. هذا يقلل الضغط على MongoDB وRender.

## إعدادات Render الاختيارية

```text
ANALYTICS_EVENT_TTL_DAYS=90
ANALYTICS_FLUSH_INTERVAL_MS=10000
ANALYTICS_ACTIVE_WINDOW_MS=150000
ANALYTICS_LOCAL_BACKUP=false
```

التحليل الذكي الحالي يعمل محلياً على المؤشرات المجمعة ويعطي ملخصات وتنبيهات عن الأخطاء، البطء، الرفع، الذاكرة، والذروة. لا يحتاج مفتاح خدمة إضافي.
