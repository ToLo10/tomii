# تحديث TOMI

## ما تم إصلاحه

- أضيف **التجميد الشامل للمنصة**: المالك أو المشرف الذي يملك صلاحية `manage_platform_bans` يستطيع تجميد الحساب مؤقتاً أو دائماً. التجميد يمنع Socket.IO وواجهات HTTP للرسائل والوسائط والمكالمات والغرف الصوتية والألعاب والعجلة وتعديل الملف الشخصي والنشر في اكسبلور.
- أضيف محدد سرعة للرسائل (`CHAT_MESSAGE_WINDOW_MS`, `CHAT_MESSAGE_LIMIT`, `CHAT_MESSAGE_MIN_INTERVAL_MS`) مع تنظيف الذاكرة، وتجميع تحديثات قائمة المحادثات في المتصفح حتى لا يتوقف السيرفر عند الإرسال المتتابع.
- أصبحت الوسائط الجديدة تعتمد على Cloudflare R2 في بيئة الإنتاج، مع مسار MongoDB/GridFS دائم كاحتياطي؛ لا يتم الاحتفاظ بنسخة دائمة على قرص الخادم أو في نسخة JSON محلية. الملف المؤقت أثناء النقل يحذف بعد نجاح التخزين الدائم، والرفع يرفض بأمان إذا لم يكن R2 ولا MongoDB/GridFS مهيأً.
- رفعت مدة روابط قراءة R2 إلى 24 ساعة، وأضيف تجديد تلقائي للرابط عند تعثر فيديو أو صوت في المحادثة. اكسبلور يحتوي أيضاً على استعادة تلقائية للرابط.
- صغرت بطاقات عجلة الروليت ووسعت المسافة بينها، وحسنت تصميم بطاقات توميرو واللوحة، وأضفت لوحة سلالم وأفاعي أكثر وضوحاً مع شارات السلم والأفعى.
- توميرو تدعم بدء المباراة بلاعبين أو أربعة لاعبين (`allowedPlayers: [2, 4]`).

## إعداد الإنتاج

يجب ضبط MongoDB أو Cloudflare R2 مع TURN/WebRTC في Render. المتغيرات المهمة موجودة في `render.yaml`، وأهمها:

```text
REQUIRE_MONGODB=true
REQUIRE_EXTERNAL_MEDIA_STORAGE=true
WRITE_LOCAL_JSON_BACKUP=false
R2_PRESIGNED_DOWNLOAD_TTL_SECONDS=86400
```

لا تفعّل `VIDEO_COMPATIBILITY_ENABLED` على خطة صغيرة إلا بعد توفير عامل ffmpeg؛ التشغيل الأصلي مع رابط R2 يعبر الأجهزة، واستعادة الرابط تعالج انتهاء الرابط أو الانقطاع المؤقت.

## الفحص

تم فحص صياغة ملفات Node وملف JavaScript المضمن في `Public/room.html`، كما تم التحقق من تعريف احتمالات الروليت ودعم توميرو للاعبين `2,4`.
